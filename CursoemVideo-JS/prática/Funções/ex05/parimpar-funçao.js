/* Crie uma função verificarPar que receba um número e retorne true se for par ou false se for ímpar. */

function VerificarPar(n) {
    if (n % 2 == 0) {
        return true
    } else {
        return false
    }
}

console.log(VerificarPar(40))