/* Crie uma função contador que receba um número e exiba todos os números de 1 até ele. */

function contador(n) {
    for(let c = 1; c <= n; c++) {
        console.log(c)
    }
}

console.log(contador(42))