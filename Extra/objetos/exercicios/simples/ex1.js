/*
Crie um objeto chamado pessoa contendo as propriedades:

nome
idade
cidade

Exiba todas as informações no console. */

const obj = {
    nome: "Pedro",
    idade: 15,
    cidade: "Rio de Janeiro",
};

console.log(obj.nome);
console.log(obj.idade);
console.log(obj.cidade);